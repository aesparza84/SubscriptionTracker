<UserControl x:Class="SubscriptionTracker.View.UserControls.ClearableTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SubscriptionTracker.View.UserControls"       
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="250">
    <Grid Background="White">
        <TextBox Name="txtInput" VerticalContentAlignment="Center" Background="Transparent" FontSize="16" FontWeight="Light" 
                 TextChanged="txtInput_TextChanged"></TextBox>

        <TextBlock x:Name="tbPlaceHolder" Foreground="DarkGray" Background="Transparent" Text="{Binding PlaceHolder}" FontSize="16" VerticalAlignment="Center" Panel.ZIndex="-1"></TextBlock>
        
        <Button Name="btnClear"
                Content="X"
                Width="30"
                HorizontalAlignment="Right"
                BorderThickness="0" 
                Background="Transparent" 
                Foreground="LightGray"
                Click="btnClear_Click"></Button>
    </Grid>
</UserControl>
