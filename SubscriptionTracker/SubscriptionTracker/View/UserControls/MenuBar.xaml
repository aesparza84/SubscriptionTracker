<UserControl x:Class="SubscriptionTracker.View.UserControls.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SubscriptionTracker.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <!--Toolbar-->
            <RowDefinition Height="40"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Menu>
            <MenuItem Header="File" FontFamily="16">
                <MenuItem Header="Exit" FontSize="10"/>
            </MenuItem>
            <MenuItem Header="Edit" FontFamily="16"/>
        </Menu>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <Button />
            <TextBox Grid.Column="1" />
            <Rectangle Fill="LightGray" Grid.Column="0"/>
            <Rectangle Fill="LightCoral" Grid.Column="1"/>
        </Grid>
    </Grid>
</UserControl>
